# SaaS readiness and evidence

Objective: deliver Afterword as a remote, multi-user service for personal Telegram, Signal, WhatsApp, and Discord message history. A website that depends on the owner's Mac is not completion.

## Required outcomes

- Hosted Telegram, Signal, and WhatsApp account linking from the authenticated website, with no local installation.
- Continuous server capture after the browser closes; persistent sessions and restart/reconnect recovery.
- Separate account/session/message state per user and source, owner-only QR/login controls, encrypted persistent credentials, cancellation, pause, disconnect, and account deletion.
- Discord personal-account capture that meets the user's remote requirement. An unofficial cloud connector is implemented; it requires live phone approval and capture verification. Discord forbids automated personal accounts, so this cannot be represented as an approved integration or a reliable public-launch commitment without resolving that restriction.
- Clear coverage, limitations, setup instructions, visible error recovery, and verified responsive onboarding.
- Registration, authentication, recovery, retention, export, limits, and the chosen access/payment model.
- Production deployment, backups with a verified restore, health monitoring, capacity boundaries, security checks, and actual live capture verification for each supported platform.

## Evidence log

- Before hosted migration: live archive runs on DigitalOcean; all three non-Discord collectors depend on local companion processes. Discord browser extension is experimental and requires an open tab.
- Hosted implementation: authenticated browser QR and prompt flows; separate worker/queue per source; encrypted sessions; automatic restart; pause, disconnect, relink, and erasure implemented.
- Automated suite: real subprocess restart/restore against simulated provider events, HTTP ownership/CSRF checks, concurrent capacity enforcement, encrypted auth stores, recovery-key rotation/replay rejection, backup/restore, native cache cleanup, and Signal shutdown ordering. Simulated providers are not evidence of live platform compatibility.
- Live 2026-09-10: existing owner Telegram and WhatsApp sessions moved to DigitalOcean. Both local processes stopped. Server reports both connected. New ordinary messages have arrived from both after migration. Telegram Saved Messages test captured create plus edit, retaining both versions. Deletion capture and WhatsApp edits still need live verification.
- Live browser check: a temporary, empty customer workspace rendered a real server-generated WhatsApp QR from the public HTTPS app. Test account and its pending collector were deleted afterward. Hosted QR layout was widened following visual inspection.
- Signal runtime: a live server probe exposed a native library that could not load from noexec tmpfs, then exceeded a 128 MB executable tmpfs. Native libraries now use a separate disk cache; the actual hosted worker successfully produces a Signal linking QR. A phone scan and live capture verification remain outstanding.
- Discord cloud implementation: phone QR approval with an RSA challenge, encrypted per-source session, strict personal-channel filtering, ordered event persistence before Gateway resume checkpoints, reconnect/revocation handling, and explicit account-risk consent. A real unauthenticated QR handshake succeeded from DigitalOcean. Tests use real WebSockets against a simulated Discord service; they do not establish live account access. Phone approval and live messages remain unverified.
- Discord onboarding verified in production: an isolated test workspace generated a real QR through the authenticated customer wizard. Desktop and 390 px mobile layouts were checked, including scrolling to the action buttons. The test source and workspace were deleted without linking a platform account. The automated suite now has 49 passing tests; the production build also passes.
- Recovery: registration issues a single-use recovery key; Settings can replace it after password confirmation; recovery rotates the key and invalidates old sessions. Email recovery is not configured.
- Backups: startup/daily online snapshots of collector queues then archive, last seven retained. A production snapshot was downloaded off the server and restored in an isolated local directory: SQLite integrity passed; all 49 messages in that snapshot and both linked sessions decrypted. DigitalOcean daily server backups are now enabled with seven-day retention; first window September 10, 20:00–00:00 UTC. A completed provider image and full-image restore remain unverified. These images include server keys and have different protection from the application snapshots; see [operations](OPERATIONS.md).
- External availability monitoring: created an Afterword-specific DigitalOcean Uptime check for the public health endpoint in US East and Western Europe; both regions report UP. Re-running provisioning reused the same check without a duplicate. Alert recipient and delivery verification remain outstanding. The monitor does not yet check backup freshness, disk capacity, or individual collectors.
- Archive reads: SQL pagination and counts, bounded historical search without a plaintext index, streaming exports, cancellation, and request concurrency limits implemented. The suite now has 58 passing tests, including edits and deletions during export. A local 12,001-message/26,000-version fixture streamed a 91.5 MB export under a 64 MB heap limit; this is not a production load guarantee. See [archive storage](ARCHIVE-STORAGE.md).
- Production archive migration, September 10: a fresh backup containing 59 messages and 73 events was downloaded and migrated in isolation; integrity and ciphertext preservation passed. After deployment, all 58 messages and 72 events fingerprinted before the backup were unchanged, SQL paging matched the full streamed export, and historical search passed. The live archive had grown to 63 messages. Telegram and WhatsApp restored their remote sessions. Server-side events received after hosted pairing include Telegram creates and edits, and WhatsApp creates; live hosted deletions remain unverified.
- Final archive release check at 17:18 UTC: production files match the tested reader, API, and store code. The live archive contains 64 messages and 78 events, prior history remains unchanged, pages match the streamed export, and previews agree with their revision counts. Telegram and WhatsApp report fresh connected heartbeats; Signal awaits linking and Discord has not been linked.
- Public privacy guide now explains the seven-day server backup retention and that full-server recovery images include decryption keys and are not encrypted by the provider at rest. Checked the deployed guide in the browser.
- Individual histories now use 30-event pages with stable revision boundaries, global version numbers, and comparisons across page boundaries. All 63 tests and the production build pass. The larger local benchmark exports 160 MB under a 64 MB heap limit, including a message with 2,000 long revisions. Browser checks covered page jumps, comparison boundaries, incoming-event notices, preserved scroll and typed page numbers, retry after a simulated failure, keyboard focus, and a 390 × 844 CSS-pixel layout.
- Returning-user sign-in now opens the archive. Starting sign-in from Connect account resumes the account picker; both paths were verified in a disposable local workspace. The public getting-started guide explains history navigation and new-event refresh.
- History release deployed and verified at 17:42 UTC: all 73 messages and 90 events fingerprinted before deployment are preserved. The live archive grew to 74 messages and 91 events; every paged history matches its full export, including global version numbers. Telegram and WhatsApp restored fresh connected heartbeats on the server. Signal awaits phone linking; Discord remains unlinked. The updated guide was checked on the public HTTPS site, and the disposable local preview was stopped.
- Capacity implementation: transactional account/global archive accounting, a production disk reserve shared with hosted queues and backups, bounded encrypted queues, retryable delivery failures, and preserved queues when capture stops. Settings displays usage; the browser flow from deleting synthetic history through resuming a stopped source delivered its queued edit. Desktop and 390 × 844 mobile layouts were checked. Relinking is blocked while any queued copies remain, including arrivals during shutdown. The companion download now includes its entire module dependency graph and passes an extracted CLI startup check.
- Capacity migration rehearsal: the 17:59 UTC production snapshot contains 79 messages and 101 events. Its isolated migrated copy passed integrity, owner-history decryption, unchanged archive/queue ciphertext, and usage reconciliation. Owner archive allowance usage is 293,448 bytes; all three saved collector queues fit their new bounds and contain no pending or rejected events. No collector was started from the snapshot.
- Capacity release verified in production: 75 automated tests and the production build pass. All 79 messages and 101 fingerprinted events remain unchanged after deployment; all 80 message histories matched the export at 18:04 UTC. A subsequent consistent database check found 105 events, correct account/global/queue counters, 301,800 charged archive bytes, and 14.5 GB of free disk. All 13 checked runtime and browser-bundle files match the tested build; the container is healthy. Telegram and WhatsApp restored connected hosted sessions. The published storage guide was checked in the browser and the disposable preview was stopped.
- Access/payment model: decided September 10. Subscription billing through Stripe Checkout with a card-free trial; capture pauses for unpaid accounts while the archive stays readable and exportable. Implemented with signature-verified webhooks, entitlement decided locally, hosted collectors suspended on lapse, retryable queueing for companions, an operator exemption, a plan card and banners in the app, and public plans and terms guides. Covered by tests against a scripted Stripe endpoint. Live Stripe keys, the product price, the webhook endpoint, and the legal entity named in the terms are still required from the business owner before the invitation gate is removed.
- Offsite backup implementation: full-file authenticated encryption, streamed S3 uploads, verified readback, manifest-last publication, immutable retry attempts, seven-day cleanup, 15-minute retries, private offline restore, and operator backup status. All 83 tests and the production build pass. The real S3 SDK completed a 32 MiB SQLite round trip through an isolated HTTP fixture under a 64 MiB heap limit.
- Offsite restore rehearsal at 18:18 UTC: the 18:17 production snapshot passed the encrypted HTTP round trip and offline restore. All four database files remained byte-for-byte unchanged; SQLite integrity, decryption of 91 messages/117 events, and 41 encrypted metadata records across the three collector queues passed. No collector process was started. This used local simulated storage, not DigitalOcean Spaces. The storage-key API returned 404; the control panel requires sign-in, which was requested. No Spaces bucket/subscription was created. Production storage activation and a real provider round trip remain outstanding.
- Backup release deployed and verified at 18:23 UTC: five runtime/package files match the tested code, S3 SDK version is pinned to 3.1129.0, and the container is healthy. The server reports `local_only` with seven local snapshots and a fresh 18:22 backup; no offsite coverage is claimed. All 91 messages and 117 pre-deployment events remain unchanged; the 93 histories in the subsequent export matched their API pages. Telegram and WhatsApp restored fresh hosted connections. External probes report UP in both regions. Free disk was 13.86 GB and application memory about 305 MiB at that check; this is an observation, not a load guarantee.

- Monitoring alerts, September 10, 21:05–21:08 UTC: the existing Uptime check was repointed from `/api/health` to `/api/monitor` after probing that the endpoint validates, so backup age, disk reserve, archive capacity, collector heartbeats, and delivery stalls now fail the external probe. `enable-alerts` created a `down_global` alert (two-minute period), an `ssl_expiry` alert (14 days), and Droplet policies for disk above 85% and memory above 90%, all emailing the verified DigitalOcean account address. Both regions report UP against the new target. No outage has occurred since, so alert delivery is configured but not yet observed.
- Provider backups: the first DigitalOcean server image completed at 20:02 UTC on September 10 (11.21 GB, seven-day retention). A full image restore has not been rehearsed because booting a restored image with live collector sessions would connect a cloned Telegram session from a second address; it requires an isolated outbound-blocked network and a planned window.
- Server maintenance verified at 21:09 UTC: pending package updates were applied, root SSH was restricted to public keys, and the Droplet was rebooted onto kernel 6.8.0-139. The public monitor answered within a minute of the reboot; Telegram and WhatsApp restored connected sessions and the interrupted Signal setup relaunched into `waiting`. Process count inside the container was 56 against the 256 limit; memory use was 441 MiB with 2 GB swap available.
- Resilience release deployed at 21:14 UTC after an independent code review found no authentication, tenant-isolation, or secret-logging defects. Changes: the collector supervisor now survives a worker that cannot be started (full runtime disk, undecryptable configuration, fork failure) and retries with backoff instead of crashing every customer's session; the server logs unhandled rejections by error code and shuts down cleanly on an uncaught exception instead of hanging; retention purges run in indexed 500-message batches with a 50 ms budget on page loads and a full pass hourly; unknown usernames pay the same scrypt cost as real ones at sign-in; and quota errors during resume return their public 507 message. 93 automated tests and the production build pass. All 146 messages and 186 events counted before deployment were unchanged afterwards, the five changed runtime files match the tested code, the new `messages_retention` index exists, the container is healthy, and Telegram and WhatsApp reconnected.
- Hosted Signal linking defect found and fixed, September 10: the owner's Signal source had reported "Could not complete sign-in" after every restart since the hosted migration. Reproduced on the production server with an instrumented copy of the worker against a copy of the source queue: signal-cli produced the link code in 1.6 s, then returned a JSON-RPC error about 120 s later because Signal closes each provisioning link after roughly two minutes, which the adapter treated as a fatal sign-in failure with the QR still advertised as valid for 170 s. The adapter now requests a fresh code when the window closes, shows a 115-second validity, and gives up after five codes so the worker's ten-minute setup deadline produces the accurate "Sign-in expired" message. Covered by a test against a scripted signal-cli. Deployed at 21:36 UTC: 146 messages and 186 events unchanged, the two changed runtime files match the tested code, Telegram and WhatsApp reconnected, and the relaunched Signal setup remained in `waiting` with a live code for more than four minutes, past the point where it previously failed. A phone scan by the account holder is still required to verify capture.
- Billing release deployed at 00:44 UTC on September 11 without Stripe keys, so production still runs ungated with the invitation code; the service logs that billing is not configured. All 197 messages and 245 events were unchanged, the three changed server files match the tested code, the new user billing columns exist, the public terms guide is served, and Telegram and WhatsApp reconnected. The plan card, trial-ended banner, and registration terms line were checked in a disposable local workspace with billing enabled against an unreachable Stripe endpoint. 100 automated tests and the production build pass.
- Capacity for 20 customers, September 11: measured per-collector memory (about 60–70 MB for Telegram and WhatsApp workers, 30 processes in total for three sources), reclaimed 5.3 GB of Docker build cache, and resized the Droplet with a graceful shutdown from 1 GB/25 GB to `s-2vcpu-4gb` with an 80 GB disk using the new `operations.py resize` command. The service answered again within about 90 seconds and all hosted sources reconnected. The hosted collector limit is now 30, the global archive budget 4 GiB with an 8 GiB disk reserve, the container process limit 1024, and its tmpfs 512 MB.

## Completion blockers

Live phone scans require the account holder. Discord's platform restrictions remain a public-launch risk, and full live access remains unverified. Still outstanding: the access/payment decision; a domain the operator owns in place of the sslip.io hostname; activation of DigitalOcean Spaces (control-panel sign-in and a paid subscription) so offsite application backups can be verified against the real provider; a full-image restore rehearsal in an isolated network; observed delivery of the configured alert emails; rotation of the DigitalOcean API token that appeared in chat; and production capacity/load measurement beyond the current single-customer pilot. These do not prevent progress on server infrastructure and the other platforms.
